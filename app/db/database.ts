/*import * as SQLite from 'expo-sqlite';

export const db = SQLite.openDatabaseSync('instituto.db');

export function criarTabela() {
  db.execSync(`
    CREATE TABLE IF NOT EXISTS usuarios (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      nome TEXT NOT NULL,
      email TEXT NOT NULL
    );
  `);
}
  */